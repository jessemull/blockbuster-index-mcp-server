# Copy to `.env` and fill in values. Never commit `.env`.
# See docs/ENVIRONMENT.md for full descriptions.

# -----------------------------------------------------------------------------
# AWS Configuration
# -----------------------------------------------------------------------------
AWS_REGION=us-west-2
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
S3_BUCKET_NAME=

# -----------------------------------------------------------------------------
# Amazon Signal
# -----------------------------------------------------------------------------
AMAZON_DYNAMODB_TABLE_NAME=
AMAZON_SLIDING_WINDOW_TABLE_NAME=

# -----------------------------------------------------------------------------
# Walmart Signal
# -----------------------------------------------------------------------------
WALMART_DYNAMODB_TABLE_NAME=
WALMART_SLIDING_WINDOW_DYNAMODB_TABLE_NAME=

# -----------------------------------------------------------------------------
# Broadband Signal
# -----------------------------------------------------------------------------
BROADBAND_DYNAMODB_TABLE_NAME=
BROADBAND_S3_BUCKET=

# -----------------------------------------------------------------------------
# Census Signal
# -----------------------------------------------------------------------------
CENSUS_DYNAMODB_TABLE_NAME=
FORCE_REFRESH=false

# -----------------------------------------------------------------------------
# BLS Signal
# -----------------------------------------------------------------------------
BLS_PROCESSED_FILES_TABLE_NAME=
BLS_STATE_DATA_TABLE_NAME=
BLS_SIGNALS_TABLE_NAME=
BLS_S3_BUCKET=

# -----------------------------------------------------------------------------
# Signal Scores / Blockbuster Index
# -----------------------------------------------------------------------------
SIGNAL_SCORES_DYNAMODB_TABLE_NAME=
BLOCKBUSTER_INDEX_DYNAMODB_TABLE_NAME=

# -----------------------------------------------------------------------------
# Scraping / retries
# -----------------------------------------------------------------------------
PUPPETEER_EXECUTABLE_PATH=
MAX_RETRIES=3
RETRY_DELAY=1000

# -----------------------------------------------------------------------------
# Logging
# -----------------------------------------------------------------------------
LOG_LEVEL=info
CW_LOG_GROUP=/aws/ecs/blockbuster-index-mcp-log-group
CW_LOG_STREAM=

# -----------------------------------------------------------------------------
# Application
# -----------------------------------------------------------------------------
NODE_ENV=development
CACHE_CONTROL=max-age=300

# -----------------------------------------------------------------------------
# Bastion SSH (optional — npm run bastion)
# -----------------------------------------------------------------------------
SSH_HOST=
SSH_USER=
SSH_PRIVATE_KEY_PATH=
